
If Statements
=============

This example shows the syntax of ``if`` statements, which look similar to the way
they are used in many other languages.  In this example, we use ``if`` statements
to implement a factorial function recursively.
